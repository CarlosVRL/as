# Asciidoc for Java Swing Demo

A bare-bones Java Swing application that uses asciidoc for in-line documentation.

## How to run the application

Build the project using Maven

```
mvn package
```

Run using the batch script

```
./run.bat
```